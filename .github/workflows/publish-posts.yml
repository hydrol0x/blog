name: Convert Markdown to HTML

on:
  push:
    paths:
      - 'posts/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2
        with:
          fetch-depth: 0 

      - name: Run Conversion Action
        uses: hydrol0x/static-site-generator@main
